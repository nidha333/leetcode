class Solution {
   removeDuplicates(List<int> nums) {
    final List a = nums.toSet().toList();
    for(int i=0; i < a.length; i++){
          nums[i] = a[i];
      }
    
      return a.length;
  }
}
  
