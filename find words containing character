class Solution {
  List<int> findWordsContaining(List<String> words, String x) {
    List<int> c =[];
    for(int i=0; i< words.length; i++){
        if(words[i].toString().contains(x)){
            c.add(i);
        }
    }
    return c;
  }
  
}
